# v1 Protocol

Smart contracts for Veilnyx v1 protocol

## Test Coverage
`pnpm run coverage`

> **Note:** Running without the `--ir-minimum` optimization flag can result in `Stack too deep` error as `forge coverage` by default runs without any optimizations.



