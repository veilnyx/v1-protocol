node_modules/
cache/
cache_hardhat/
out/
.env
artifacts/
# Dry-run deployment records (fork addresses under a real chain id) — real ones are tracked
deployments/*Fork-*.json
broadcast/
typechain/
typechain-types/
.DS_STORE
# .openzeppelin/ intentionally tracked — the manifest is the storage layout
# source of truth for upgrade validation. Never gitignore it.
.claude/
.vscode/
